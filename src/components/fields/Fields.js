import "./Fields.css";

export const MultiChoice = ({title}) => {
    return (
        <div className="Field">
            <h2>
                { title }
            </h2>
        </div>
    );
};

export const MultiSelect = ({title}) => {
    return (
        <div className="Field">
            <h2>
                { title }
            </h2>
        </div>
    );
};

export const ShortText = ({title}) => {
    return (
        <div className="Field">
            <h2>
                { title }
            </h2>
        </div>
    );
};